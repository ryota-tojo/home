## カテゴリー

```mermaid
classDiagram
    class Category {
        + id
        + groups_id
        + category_no
        + category_name
    }

    class Budgets {
        + id
        + groups_id
        + bg_yyyy
        + bg_mm
        + bg_category_no ★
        + bg_amount
        + fixed_flg
    }
    
    class Shopping {
        + id
        + groups_id
        + user_id
        + shopping_date
        + member_no
        + category_no ★
        + type
        + payment
        + settlement
        + amount
        + remarks
        + fixed_flg
    }
    
    class ShoppingInput {
        + id
        + groups_id
        + tmpi_name
        + tmpi_member_no
        + tmpi_category_no ★
        + tmpi_type
        + tmpi_payment
        + tmpi_settlement
        + tmpi_amount
        + tmpi_remarks
        + tmpi_use_flg
    }

    class ShoppingSearch {
        + id
        + groups_id
        + tmps_name
        + tmps_member_no
        + tmps_category_no ★
        + tmps_type
        + tmps_payment
        + tmps_settlement
        + tmps_amount
        + tmps_remarks
        + tmps_use_flg
    }

    class ShoppingEntry {
        + id
        + groups_id
        + tmpe_name
        + tmpe_member_no
        + tmpe_category_no ★
        + tmpe_type
        + tmpe_payment
        + tmpe_settlement
        + tmpe_amount
        + tmpe_remarks
        + tmpe_use_flg
    }

    Category --|> Budgets:連動
    Category --|> Shopping:連動
    Category --|> ShoppingInput:連動
    Category --|> ShoppingSearch:連動
    Category --|> ShoppingEntry:連動
```
