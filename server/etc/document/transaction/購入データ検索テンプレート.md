## 購入データ検索テンプレート

```mermaid
classDiagram
    class ShoppingSearch {
        + id
        + groups_id
        + template_id
        + tmps_name
        + tmps_member_id
        + tmps_category_id
        + tmps_type
        + tmps_payment
        + tmps_settlement
        + tmps_amount
        + tmps_remarks
        + tmps_use_flg
    }

    class Member {
        + id
        + groups_id
        + member_no
        + member_name
    }

    class Category {
        + id
        + groups_id
        + category_no
        + category_name
    }

    class Choices {
        + id
        + item_type
        + item_no
        + item_name_pc
        + item_name_sp
    }

    ShoppingSearch --|> Member:参照\n・メンバー番号
    ShoppingSearch --|> Category:参照\n・カテゴリー番号
    ShoppingSearch --|> Choices:参照\n・購入種別\n・支払い方法\n・精算状況

```
